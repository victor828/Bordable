import { inter } from "../../ui/fonts";

export function BoardFinal() {
  /* // TODO: 
      crear la lista de paleta de color para los colorres especificados
      definir el olor dependiendo del color seleccionado en el imput este viene de la bas e de datos
      
    */
  return (
    <button className={`${inter} w-64 h-36 p-4 bg-gray-300 rounded-lg`}>
      title
    </button>
  );
}
